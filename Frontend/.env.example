# Copia este archivo como .env y completa los valores.
# .env no se sube al repositorio — .env.example sí, como documentación.

# URL base de la API del backend
VITE_API_URL=http://localhost:3000/api
